{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Zig App",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "terminal": "integrated",
      "preLaunchTask": "zig: build"
    }
  ]
}
