{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug Main",
			"type": "node",
			"request": "launch",
			"program": "${workspaceFolder}/filedb",
			"preLaunchTask": "buildAndMove",
			"MIMode": "gdb",
			"cwd": "${workspaceFolder}"
		}
	]
}